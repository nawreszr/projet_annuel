apiVersion: v1
kind: Service
metadata:
  name: ai-tutor-service
  namespace: learning-portal
  labels:
    app: ai-tutor-service
spec:
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP
    name: http
  selector:
    app: ai-tutor-service
  type: ClusterIP
