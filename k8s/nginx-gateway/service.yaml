apiVersion: v1
kind: Service
metadata:
  name: nginx-gateway
  namespace: learning-portal
  labels:
    app: nginx-gateway
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx-gateway
  type: LoadBalancer
