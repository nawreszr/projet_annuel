apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: learning-portal
  labels:
    app: user-service
spec:
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: user-service
  type: ClusterIP
